---
title: 'Time series'
description: 'This section explains how to create time series dashboard elements and add them to your dashboard.'
keywords: ['axiom documentation', 'documentation', 'axiom', 'dashboards', 'collaboration', 'queries', 'visualizations', 'filters', 'charts', 'simple query builder', 'advanced query builder']
---

import Prerequisites from "/snippets/dashboard-prerequisites.mdx"
import CreateElement from "/snippets/create-dashboard-element.mdx"

Time series charts show the change in your data over time which can help identify infrastructure issues, spikes, or dips in the data. This can be a simple line chart, an area chart, or a bar chart. A time series chart might be used to show the change in the volume of log events, error rates, latency, or other time-sensitive data.

<Prerequisites />

<CreateElement elementName="time series" elementButtonLabel="Timeseries"/>

## Example with Builder

<Frame>
  <img src="/doc-assets/shots/timeseries-simple-chart.png" alt="Time series example with Builder" />
</Frame>

## Example with APL

```kusto
['sample-http-logs']
| summarize count() by bin_auto(_time)
```

<Frame>
  <img src="/doc-assets/shots/timeseries-chart-apl.png" alt="Time series example with APL" />
</Frame>