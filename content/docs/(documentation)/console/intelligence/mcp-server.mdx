---
title: Axiom MCP Server
description: "Connect AI agents to Axiom with the Model Context Protocol (MCP) Server."
sidebarTitle: Overview
keywords: ["MCP", "Model Context Protocol", "MCP server", "Axiom MCP", "Claude MCP", "Cursor MCP", "AI agent"]
---

import Prerequisites from "@/content/snippets/minimal-prerequisites.mdx"
import ReplaceToken from "@/content/snippets/replace-token.mdx"
import PrerequisiteToken from "@/content/snippets/prerequisite-token.mdx"
import ReplaceOrgId from "@/content/snippets/replace-org-id.mdx"
import ReplaceDomain from "@/content/snippets/replace-domain.mdx"

Axiom MCP Server is a [Model Context Protocol](https://modelcontextprotocol.io/) server implementation that enables AI agents to query your data using Axiom Processing Language (APL).

For guidance on when to use Axiom MCP Server and Axiom Skills, see [AI agents](/console/intelligence/ai-agents-overview).

For the full list of tools and prompts with example queries, see [MCP tools reference](/console/intelligence/mcp-server/tools).

If your agent doesn’t have an Axiom organization to work in yet, it can provision a temporary one itself and hand you a claim link later. For more information, see [Agent-created organizations](/console/intelligence/agent-created-orgs).

## Current capabilities

Axiom MCP Server works with AI agents that support MCP. It exposes tools for querying data, exploring datasets and metrics, and managing dashboards, monitors, and notifiers, plus prebuilt prompts for common investigations.

The Axiom-hosted MCP Server is located in the US. When you query your data using a remote MCP connection, query results are routed through US infrastructure.

<Prerequisites />

## Setup

**Remote MCP URL:** `https://mcp.axiom.co/mcp`

Remote MCP with OAuth is the recommended setup for most use cases.

### Install with add-mcp

Use the [`add-mcp`](https://add-mcp.com/docs/quickstart) CLI to add Axiom MCP Server to your coding agents:

```bash
npx -y add-mcp https://mcp.axiom.co/mcp -g
```

Authenticate in your browser when the agent prompts you.

Optional flags:

- Omit `-g` to install for the current project only.
- Add `-y` to skip confirmation prompts.
- Target specific agents with `-a`, for example `-a cursor -a claude-code`.

### Quick setup

Axiom MCP Server works with MCP-compatible clients. Setup steps for common clients:

- Claude.ai and Claude Desktop
- Claude Code
- Cursor
- VS Code with Copilot
- Codex CLI
- ChatGPT
- Windsurf
- Gemini CLI and Gemini Code Assist
- Goose

For client-specific steps without `add-mcp`:

<Tabs>
<Tab title="Claude">

<Tabs>
<Tab title="Pro, Max, Team, or Enterprise">

1. [Add Axiom to Claude](https://claude.ai/customize/connectors?modal=add-custom-connector&connectorName=Axiom&connectorUrl=https%3A%2F%2Fmcp.axiom.co%2Fmcp).
1. Review the prefilled name and URL, then confirm.
1. Click **Connect** and authenticate in your browser.

On Team or Enterprise plans, an organization admin may need to add the connector first. For more information, see the [Claude documentation](https://support.anthropic.com/en/articles/11175166-getting-started-with-custom-connectors-using-remote-mcp#h_3d1a65aded).

</Tab>
<Tab title="Free">

On the Free plan, use Claude Desktop with `mcp-remote`:

1. Install [Claude Desktop](https://claude.ai/download).
1. In Claude Desktop, go to **Settings > Developers**, and then click **Edit Config**.
1. Add the following to `claude_desktop_config.json`:

    ```json claude_desktop_config.json
    {
      "mcpServers": {
        "axiom": {
          "command": "npx",
          "args": ["-y", "mcp-remote", "https://mcp.axiom.co/mcp"]
        }
      }
    }
    ```

1. Restart Claude Desktop.
1. Authenticate in your browser.

</Tab>
</Tabs>

</Tab>
<Tab title="Claude Code">

```bash
claude mcp add --transport http axiom https://mcp.axiom.co/mcp
```

In Claude Code, run `/mcp` and authenticate in your browser.

</Tab>
<Tab title="Cursor">

1. [Add Axiom to Cursor](https://cursor.com/en/install-mcp?name=axiom&config=eyJ1cmwiOiJodHRwczovL21jcC5heGlvbS5jby9tY3AifQ%3D%3D).
1. Authenticate in your browser.

Optional: add the following to your Cursor MCP config instead:

```json
{
  "mcpServers": {
    "axiom": {
      "url": "https://mcp.axiom.co/mcp"
    }
  }
}
```

To add Axiom MCP Server for your Cursor team, go to **Dashboard > Integrations & MCP** in Cursor, and then add Axiom MCP Server using the URL `https://mcp.axiom.co/mcp`. To make it available to Cursor cloud agents, you must also make it available by default on **Dashboard > Plugins**.

</Tab>
<Tab title="VS Code">

1. [Add Axiom to VS Code](vscode:mcp/install?%7B%22name%22%3A%22axiom%22%2C%22type%22%3A%22http%22%2C%22url%22%3A%22https%3A//mcp.axiom.co/mcp%22%7D).
1. Authenticate in your browser.

Optional: add the following to `.vscode/mcp.json` instead:

```json
{
  "servers": {
    "axiom": {
      "type": "http",
      "url": "https://mcp.axiom.co/mcp"
    }
  }
}
```

</Tab>
<Tab title="Codex">

```bash
codex mcp add axiom --url https://mcp.axiom.co/mcp
```

Codex detects OAuth and opens your browser to authorize the connection.

</Tab>
<Tab title="ChatGPT">

Custom connectors are available on ChatGPT for Pro and Plus plans on the web.

1. Go to **Settings > Connectors > Advanced settings** and enable **Developer mode**.
1. In the **Connectors** tab, create a connector:
    - **Name:** `Axiom`
    - **MCP server URL:** `https://mcp.axiom.co/mcp`
    - **Authentication:** `OAuth`
1. Click **Create**.
1. Authenticate in your browser.

</Tab>
<Tab title="Windsurf">

Add the following to your Windsurf `mcp_config.json`:

```json
{
  "mcpServers": {
    "axiom": {
      "serverUrl": "https://mcp.axiom.co/mcp"
    }
  }
}
```

Restart Windsurf, then authenticate in your browser.

</Tab>
<Tab title="Gemini">

Add the following to `~/.gemini/settings.json` for Gemini CLI or Gemini Code Assist:

```json
{
  "mcpServers": {
    "axiom": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://mcp.axiom.co/mcp"]
    }
  }
}
```

Restart the client, then authenticate in your browser. In Gemini CLI, run `/mcp list` to confirm the server is available.

</Tab>
<Tab title="Goose">

1. [Add Axiom to Goose](goose://extension?url=https%3A%2F%2Fmcp.axiom.co%2Fmcp&type=streamable_http&id=axiom&name=Axiom&description=Query%20and%20analyze%20Axiom%20data%20with%20APL%20from%20your%20AI%20agent).
1. Authenticate in your browser when prompted.

Or start a Goose session with the remote server:

```bash
goose session --with-streamable-http-extension "https://mcp.axiom.co/mcp"
```

</Tab>
<Tab title="Other">

If your agent supports remote MCP over HTTP:

- **Name:** `Axiom`
- **URL:** `https://mcp.axiom.co/mcp`

If your agent doesn’t support remote MCP directly, use [`mcp-remote`](https://www.npmjs.com/package/mcp-remote):

```json
{
  "mcpServers": {
    "axiom": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://mcp.axiom.co/mcp"]
    }
  }
}
```

Restart the agent, then authenticate in your browser.

For legacy clients that only support the older HTTP+SSE transport, use `https://mcp.axiom.co/sse` instead.

</Tab>
</Tabs>

### Header-based authentication

If your AI agent doesn’t support browser-based OAuth (for example, Agent Builder from OpenAI), authenticate with headers:

1. [Create a personal access token](/reference/tokens#personal-access-tokens-pat).
1. [Determine organization ID](/reference/tokens#determine-organization-id).
1. Configure your AI agent to use `https://mcp.axiom.co/mcp`.
1. Send these headers with each request:

    - **Authorization:** `Bearer PERSONAL_ACCESS_TOKEN`
    - **x-axiom-org-id:** `ORGANIZATION_ID`

    <Info>
    Replace `PERSONAL_ACCESS_TOKEN` with the personal access token you have generated.

    <ReplaceOrgId />
    </Info>

    If your agent only supports the Authorization header, pass the organization ID as a URL parameter: `https://mcp.axiom.co/mcp?org-id=ORGANIZATION_ID`

### Local setup

Optional: run Axiom MCP Server locally for more control. With the local setup:

- You run the server yourself.
- You authenticate with an API token instead of OAuth 2.0.
- You can’t use the MCP [prompts](/console/intelligence/mcp-server/tools#prompts).

<Accordion title="Local setup">

#### Install

Run the following to install the latest built binary from [GitHub](https://github.com/axiomhq/axiom-mcp/releases):

```bash
go install github.com/axiomhq/axiom-mcp@latest
```

#### Create API token

<PrerequisiteToken />

#### Configure

Configure your AI agent with a [config file](#config-file) or [environment variables](#environment-variables).

##### Config file

1. Create a config file where you specify the authentication and configuration details. For example:

    ```bash config.txt
    token API_TOKEN
    url https://api.axiom.co
    query-rate 1
    query-burst 1
    datasets-rate 1
    datasets-burst 1
    monitors-rate 1
    monitors-burst 1
    ```

    <Info>
    <ReplaceToken />
    </Info>

    Optionally, configure the rate and the burst limits at the query, dataset, and monitor levels. The rate limit is the maximum number of requests that your AI agent can make per second. The burst limit is the maximum number of requests that your AI agent can make in a short period (burst) before the rate limit applies to further requests.

1. In the settings of your AI agent, reference the binary file of Axiom MCP Server and the config file you have previously created.

    For example, if you use the Claude desktop app, add `axiom` to the `mcpServers` section of the Claude configuration file. The default path is `~/Library/Application Support/Claude/claude_desktop_config.json` on MacOS and `%APPDATA%\Claude\claude_desktop_config.json` on Windows.

    ```json claude_desktop_config.json
    {
      "mcpServers": {
        "axiom": {
          "command": "PATH_AXIOM_MCP_BINARY",
          "args": [
            "--config",
            "PATH_AXIOM_MCP_CONFIG"
          ]
        }
      }
    }
    ```

    <Info>
    Replace `PATH_AXIOM_MCP_BINARY` with the path the binary file of Axiom MCP Server.

    Replace `PATH_AXIOM_MCP_CONFIG` with the path the config file you have previously created.
    </Info>

##### Environment variables

In the settings of your AI agent, use environment variables to specify the authentication details and the binary file of Axiom MCP Server.

For example, if you use the Claude desktop app, add `axiom` to the `mcpServers` section of the Claude configuration file. The default path is `~/Library/Application Support/Claude/claude_desktop_config.json` on MacOS and `%APPDATA%\Claude\claude_desktop_config.json` on Windows.

```json claude_desktop_config.json
{
  "mcpServers": {
    "axiom": {
      "command": "PATH_AXIOM_MCP_BINARY",
      "env": {
        "AXIOM_TOKEN": "API_TOKEN",
        "AXIOM_URL": "https://api.axiom.co",
        "AXIOM_QUERY_RATE": "1",
        "AXIOM_QUERY_BURST": "1",
        "AXIOM_DATASETS_RATE": "1",
        "AXIOM_DATASETS_BURST": "1",
        "AXIOM_MONITORS_RATE": "1",
        "AXIOM_MONITORS_BURST": "1"
      }
    }
  }
}
```

<Info>
Replace `PATH_AXIOM_MCP_BINARY` with the path the binary file of Axiom MCP Server.

<ReplaceToken />

<ReplaceDomain />
</Info>

Optionally, configure the rate and the burst limits at the query, dataset, and monitor levels. The rate limit is the maximum number of requests that your AI agent can make per second. The burst limit is the maximum number of requests that your AI agent can make in a short period (burst) before the rate limit applies to further requests.

</Accordion>

## Token hygiene

The remote MCP Server uses OAuth for authentication which handles credential isolation automatically. Agents never see your tokens. You can [revoke access](#revoke-access) at any time.

For the local setup or when using [Axiom Skills](/console/intelligence/skills), you configure API tokens directly. For detailed guidance on secure token configuration, see [Token hygiene for AI agents](/console/intelligence/ai-agents-overview#token-hygiene-for-ai-agents).

## Limit query costs

Query cost limits let you cap the hourly and daily query costs of an AI agent so it can query your data autonomously without creating unexpected costs:

- For the local setup, set query cost limits on the API token you create for the agent.
- For the remote MCP Server, the agent acts as your user account through OAuth, so set query cost limits on your roles instead.

For more information, see [Set query cost limits for AI agents](/console/intelligence/query-cost-limits).

## Use MCP Server

After setup, ask your agent about your data. For example:

- “List datasets.”
- “List the fields in the dataset `logs`.”
- “Get the most common status codes in the last 30 minutes in the dataset `logs`.”

Optional: use the [prebuilt prompts](/console/intelligence/mcp-server/tools#prompts). In Claude, click **+ > Add from Axiom** to access them.

For more examples and the full tool list, see [MCP tools reference](/console/intelligence/mcp-server/tools).

## Revoke access

To revoke access:

1. In Axiom, go to <Icon icon="gear" iconType="solid"/> **Settings > Profile**.
1. In the Sessions section, find the session where you authorized your AI client, and then click <Icon icon="trash" iconType="light"/> **Delete** on the right.

If you connected with the `mcp-remote` library, clear the `.mcp-auth` folder. On a Mac, the default path is `~/.mcp-auth`.
