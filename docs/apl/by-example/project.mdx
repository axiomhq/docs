---
dataset: [{
  statusCode: 200,
  path: '/',
  method: 'GET',
  durationMs: 213
}, {
  statusCode: 400,
  path: '/login',
  method: 'POST',
  durationMs: 445
}]
query: |-
  example
  | project method, path, durationHuman = strcat(tostring(durationMs), "ms")
---

# Project

Select the fields to include in the result. You can also create new fields by 
combining project with a function.

Try it out for yourself below!

<AplByExampleEditor />

To learn more, see the [project](/docs/apl/tabular-operators/project-operator) docs.
